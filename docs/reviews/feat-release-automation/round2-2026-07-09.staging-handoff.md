# feat-release-automation - Round 2 Staging Handoff

- Date: 2026-07-09
- Level: DevOps / release governance correction
- Scope: staging deployment evidence and PR-only production boundary

## Intent

Consolidate the release work completed up to staging and correct the governance
language that could be read as allowing direct production branch pushes.

## Findings

- The staging deployment path is working for commit
  `4e2ae53865e138bc166429e4e263d803e7e60960`.
- `Deploy Staging` succeeded on branch `stg`.
- The operator verified `https://stg.t3s7.com` in a browser.
- The previous documentation was too loose around production promotion. It did
  not make the PR boundary explicit enough before `main`.
- GitHub Environment branch policies do not replace branch protection. They
  restrict deployments, not Git writes to `main`.

## Changes

| File | Change |
| --- | --- |
| `docs/features/staging/release-handoff-2026-07-09.md` | Added staging handoff record with commit, checks, browser verification, and production boundary. |
| `docs/features/staging/operations.md` | Linked the staging handoff and clarified that agent-operated deployment stops at `stg`. |
| `docs/features/fumadocs/release-automation.md` | Changed production promotion from direct promotion language to PR-based promotion. |
| `docs/features/fumadocs/operations.md` | Clarified that production branch updates must go through PR and that Environment policies are not branch protection. |
| `docs/architecture/release-governance.md` | Updated the architecture path to `feature/* -> stg -> PR -> main`. |

## Staging Evidence

```text
Commit: 4e2ae53865e138bc166429e4e263d803e7e60960
Feature branch CI: success
stg branch CI: success
Deploy Staging: success
Staging deployment: success
Browser verification: passed by operator on https://stg.t3s7.com
```

## Remaining Governance Work

- Add branch protection or a repository ruleset for `main`.
- Require pull requests before merging into `main`.
- Block direct pushes to `main`.
- Require CI and review before merge.
- Keep production Environment approval as a second gate, not a substitute for
  PR review.
